package org.vidyq.client.enums;

public enum ComparisonEnum {

	EQ, //equals 
	NEQ,  //notequals
	GT, //greater than
	GET, //greater equals than
	LT,// less than
	LET, //less equals than
	LIKE; //like
}
